// $('.slick-list').addClass('row');

document.addEventListener('keydown', function (event) {
     if (event.keyCode == 123) {
          event.preventDefault(); 
          alert('adu hacker! code by vudevweb');
          return false;
     }
});

document.addEventListener('contextmenu', function (event) {
     event.preventDefault();
     alert('adu hacker! code by vudevweb');
});